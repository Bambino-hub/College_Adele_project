{% extends 'base.html.twig' %}

{% block title %}Détail du niveau
{% endblock %}

{% block body %}
	<div class="container py-4 crud-page">
		<div class="crud-shell crud-shell--narrow">
			<div class="crud-hero crud-hero--soft">
				<span class="crud-hero__eyebrow">Consultation</span>
				<h1 class="crud-hero__title">Détail du niveau</h1>
				<p class="crud-hero__subtitle">Affichez les informations clés de ce niveau scolaire.</p>
			</div>

			<div class="crud-card">
				<table class="table crud-detail-table">
					<tbody>
						<tr>
							<th>Identifiant</th>
							<td>{{ niveau.id }}</td>
						</tr>
						<tr>
							<th>Nom</th>
							<td>{{ niveau.name }}</td>
						</tr>
					</tbody>
				</table>
			</div>

			<div class="crud-actions">
				<div class="d-flex flex-wrap gap-2">
					<a href="{{ path('app_niveau_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
					<a href="{{ path('app_niveau_edit', {'id': niveau.id}) }}" class="btn btn-outline-primary">Modifier</a>
				</div>
				{{ include('niveau/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}

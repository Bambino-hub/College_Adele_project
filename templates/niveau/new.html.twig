{% extends 'base.html.twig' %}

{% block title %}Créer un niveau
{% endblock %}

{% block body %}
	<div class="container py-4 crud-page">
		<div class="crud-shell crud-shell--narrow">
			<div class="crud-hero">
				<span class="crud-hero__eyebrow">Organisation académique</span>
				<h1 class="crud-hero__title">Ajouter un niveau</h1>
				<p class="crud-hero__subtitle">Créez un nouveau niveau pour organiser plus finement les classes et les séries.</p>
			</div>

			{{ include('niveau/_form.html.twig', {'finish_path': path('app_niveau_index')}) }}
		</div>
	</div>
{% endblock %}

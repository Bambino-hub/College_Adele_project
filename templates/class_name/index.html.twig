{% extends 'base.html.twig' %}

{% block title %}ClassName index{% endblock %}

{% block body %}
    <h1>ClassName index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Cycle</th>
                <th>Niveau</th>
                <th>Serie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for class_name in class_names %}
            <tr>
                <td>{{ class_name.id }}</td>
                <td>{{ class_name.name }}</td>
                <td>{{ class_name.niveau and class_name.niveau.cycle ? class_name.niveau.cycle.name : '-' }}</td>
                <td>{{ class_name.niveau ? class_name.niveau.name : '-' }}</td>
                <td>{{ class_name.serie ? class_name.serie.name : '-' }}</td>
                <td>
                    <a href="{{ path('app_class_name_show', {'id': class_name.id}) }}">show</a>
                    <a href="{{ path('app_class_name_edit', {'id': class_name.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_class_name_new') }}">Create new</a>
{% endblock %}

<div class="teachers-form">
	{{ form_start(form, {'attr': {'class': 'teachers-form-element'}}) }}
	{{ form_widget(form) }}
	<button class="btn btn-primary teachers-submit-btn">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}
</div>

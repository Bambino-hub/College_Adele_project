<form method="post" action="{{ path('teatchers_delete', {'id': teatcher.id}) }}" onsubmit="return confirm('Supprimer cet enseignant ?');" class="teachers-delete-form d-inline">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ teatcher.id) }}">
	<button class="btn btn-outline-danger teachers-delete-btn">Supprimer</button>
</form>

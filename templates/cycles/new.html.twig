{% extends 'base.html.twig' %}

{% block title %}Créer un cycle
{% endblock %}

{% block body %}
	<div class="container py-4 crud-page">
		<div class="crud-shell crud-shell--narrow">
			<div class="crud-hero">
				<span class="crud-hero__eyebrow">Structure scolaire</span>
				<h1 class="crud-hero__title">Ajouter un cycle</h1>
				<p class="crud-hero__subtitle">Créez un nouveau cycle pour mieux organiser les niveaux et les classes de l’établissement.</p>
			</div>

			{{ include('cycles/_form.html.twig', {'finish_path': path('app_cycles_index')}) }}
		</div>
	</div>
{% endblock %}

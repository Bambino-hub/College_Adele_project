{% extends 'base.html.twig' %}

{% block title %}Détail du cycle
{% endblock %}

{% block body %}
	<div class="container py-4 crud-page">
		<div class="crud-shell crud-shell--narrow">
			<div class="crud-hero crud-hero--soft">
				<span class="crud-hero__eyebrow">Consultation</span>
				<h1 class="crud-hero__title">Détail du cycle</h1>
				<p class="crud-hero__subtitle">Retrouvez les informations principales du cycle sélectionné.</p>
			</div>

			<div class="crud-card">
				<table class="table crud-detail-table">
					<tbody>
						<tr>
							<th>Identifiant</th>
							<td>{{ cycle.id }}</td>
						</tr>
						<tr>
							<th>Nom</th>
							<td>{{ cycle.name }}</td>
						</tr>
					</tbody>
				</table>
			</div>

			<div class="crud-actions">
				<div class="d-flex flex-wrap gap-2">
					<a href="{{ path('app_cycles_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
					<a href="{{ path('app_cycles_edit', {'id': cycle.id}) }}" class="btn btn-outline-primary">Modifier</a>
				</div>
				{{ include('cycles/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}

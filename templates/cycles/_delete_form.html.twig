<form class="d-inline" method="post" action="{{ path('app_cycles_delete', {'id': cycle.id}) }}" onsubmit="return confirm('Supprimer ce cycle ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cycle.id) }}">
	<button class="btn btn-outline-danger">Supprimer</button>
</form>

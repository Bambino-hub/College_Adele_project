{% extends 'base.html.twig' %}

{% block title %}Créer un enseignement
{% endblock %}

{% block body %}
	<div class="container py-4 crud-page">
		<div class="crud-shell crud-shell--narrow">
			<div class="crud-hero">
				<span class="crud-hero__eyebrow">Pédagogie</span>
				<h1 class="crud-hero__title">Ajouter un enseignement</h1>
				<p class="crud-hero__subtitle">Associez un enseignant, une matière et une classe dans une interface claire et professionnelle.</p>
			</div>

			{{ include('enseignement/_form.html.twig', {'finish_path': path('app_enseignement_index')}) }}
		</div>
	</div>
{% endblock %}

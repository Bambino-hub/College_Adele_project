{% extends 'base.html.twig' %}

{% block title %}Modifier un enseignement
{% endblock %}

{% block body %}
	<div class="container py-4 crud-page">
		<div class="crud-shell crud-shell--narrow">
			<div class="crud-hero crud-hero--soft">
				<span class="crud-hero__eyebrow">Mise à jour</span>
				<h1 class="crud-hero__title">Modifier un enseignement</h1>
				<p class="crud-hero__subtitle">Mettez à jour l’affectation pédagogique sélectionnée.</p>
			</div>

			{{ include('enseignement/_form.html.twig', {'button_label': 'Mettre à jour', 'finish_path': path('app_enseignement_index')}) }}

			<div class="crud-actions">
				<a href="{{ path('app_enseignement_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
				{{ include('enseignement/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}

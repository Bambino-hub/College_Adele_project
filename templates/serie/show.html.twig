{% extends 'base.html.twig' %}

{% block title %}Détail de la série
{% endblock %}

{% block body %}
	<div class="container py-4 crud-page">
		<div class="crud-shell crud-shell--narrow">
			<div class="crud-hero crud-hero--soft">
				<span class="crud-hero__eyebrow">Consultation</span>
				<h1 class="crud-hero__title">Détail de la série</h1>
				<p class="crud-hero__subtitle">Affichez les informations enregistrées pour cette série.</p>
			</div>

			<div class="crud-card">
				<table class="table crud-detail-table">
					<tbody>
						<tr>
							<th>Identifiant</th>
							<td>{{ serie.id }}</td>
						</tr>
						<tr>
							<th>Nom</th>
							<td>{{ serie.name }}</td>
						</tr>
					</tbody>
				</table>
			</div>

			<div class="crud-actions">
				<div class="d-flex flex-wrap gap-2">
					<a href="{{ path('app_serie_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
					<a href="{{ path('app_serie_edit', {'id': serie.id}) }}" class="btn btn-outline-primary">Modifier</a>
				</div>
				{{ include('serie/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
